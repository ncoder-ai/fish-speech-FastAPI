# Extra deps for the OpenAI-compatible API layer (on top of `uv sync --extra cu128`).
# Install with:  uv pip install -r requirements-openai-api.txt
fastapi>=0.115
python-multipart>=0.0.9
# `openai` is only needed to RUN the client-compatibility tests, not the server.
openai>=1.40
# Auto-transcribes reference voices enrolled without a transcript. A transcript
# is REQUIRED for good voice cloning and for multi-speaker voice_map binding
# (empty transcript -> all speakers collapse to one voice). Optional: the server
# degrades to empty transcripts if absent. Runs CPU int8 by default (set
# FISH_ASR_DEVICE=cuda:N to use the GPU).
faster-whisper>=1.1
# Optional: only needed for --quantize int8/int4 (weight-only). For int4 on
# torch 2.8 the tile_packed (aten tinygemm) path is used; default plain/preshuffled
# int4 needs torch>=2.11. int4 requires bf16 (server auto-disables --half).
torchao>=0.17
